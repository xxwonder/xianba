<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sampson.yjj.xianba">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".homepage.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".news.NewsActivity" />
        <activity android:name=".xiaohua.XiaoHuaActivity" />
        <activity android:name=".xiaohua.ShowImageActivity" />
        <activity android:name=".news.NewsDetailActivity" />
        <activity android:name=".firstname.FirstNameActivity" />
        <activity android:name=".firstname.XingShiSearchActivity" />
        <activity android:name=".todayhistory.HistoryToday" />
        <activity android:name=".todayhistory.SearchHistoryDayActivity" />
        <activity android:name=".famouspeople.MingRenMingYanActivity"></activity>
    </application>

</manifest>